import { CambioColorDirective } from './cambio-color.directive';

describe('CambioColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioColorDirective();
    expect(directive).toBeTruthy();
  });
});
